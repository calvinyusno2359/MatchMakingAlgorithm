// unit test for Queue object
const assert = require('chai').assert;
const queue = require("../handlers/queue");

describe('Queue Test: Methods', () => {
  it("Queue is inititalized properly.", () => {
    assert.isTrue(true);
  });

});

describe('Queue Test: Scenarios', () => {
  it("Queue is inititalized properly.", () => {
    assert.isTrue(true);
  });

});
