// unit test for Queue object
const assert = require('chai').assert;
const queue = require("../handlers/queue");

describe('Queue Test 1', () => {
  it("blah a", () => {
    assert.isTrue(true);
  });

});
